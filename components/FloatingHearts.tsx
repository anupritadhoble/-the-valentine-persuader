
import React from 'react';

// This component is now effectively a placeholder as the user requested a static page.
const FloatingHearts: React.FC = () => {
  return null;
};

export default FloatingHearts;
